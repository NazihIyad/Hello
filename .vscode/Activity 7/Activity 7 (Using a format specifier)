#include <stdio.h>

int product_prices[] = {13, 140, 900, 2};

int main(void) {
    int size = sizeof(product_prices) / sizeof(product_prices[0]); // get array length
    for (int i = 0; i < size; ++i) {
        printf("%d: %d\n", i, product_prices[i]); 
    }
    return 0;
}
